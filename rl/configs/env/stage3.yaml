# @package env
# Stage 3: full item set (speed buff, multiplier charge), denser obstacles,
# slightly scarcer gold. Final curriculum stage.

stage: 3
n_envs: 64

seed: null
max_episode_steps: null


# obs is already scaled to [0,1]/[-1,1] in sim/src/engine/obs.rs; per-dim
# VecNormalize over sparse binary CNN planes injects scale noise — keep off.
normalize_obs: false
normalize_reward: true
