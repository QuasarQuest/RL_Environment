# @package env
# Stage 3: full item set (health, ammo, speed), mixed obstacles.

stage: 3
n_envs: 48

seed: null
max_episode_steps: null

clip_reward: true
clip_reward_max: 10.0
reward_scale: 1.0

normalize_obs: true
normalize_reward: true
