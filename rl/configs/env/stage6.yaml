# @package env
# Stage 6: full combat, self-play mode.
# The sim uses BehaviorTree as placeholder; the training script loads a frozen
# RL checkpoint as the opponent (see train_selfplay.py when implemented).

stage: 6
n_envs: 32

seed: null
max_episode_steps: null

clip_reward: true
clip_reward_max: 100.0
reward_scale: 1.0

# obs is already scaled to [0,1]/[-1,1] in sim/src/engine/obs.rs; per-dim
# VecNormalize over sparse binary CNN planes injects scale noise — keep off.
normalize_obs: false
normalize_reward: true
